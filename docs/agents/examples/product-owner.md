# ProductOwner — Complementos

## Responsabilidades adicionais
- Consultar `docs/ai-ready/context-index.yml` e demais fontes para formar contexto.
- Quando houver dependências técnicas, reforçar que o padrão de DI/estado do time é `provider` (ver `docs/agents/agent-guidelines.md`).

## Colaboração
- Receber guardrails técnicos de `CleanCodeArchitect` e `FlutterEspecialist`.
- Alinhar prioridades e disponibilidade com `ScrumMaster`.
- Garantir insumos de UX (quando necessários) com `UIDesigner`.

## Referências adicionais (quando existir)
- `docs/agents/commands.md`.
- `docs/process/dor-dod.md`.
- `docs/ai-ready/context-index.yml`.
